/* Resolution timeline (from the moment the right card is tapped):
 *   0.00s   card begins flipping
 *   0.55s   flip complete, plain-white answer visible
 *   1.55s   highlight (green/red) + shake kick in
 *   3.55s   cards flip back / settle to matched
 */
export const SUCCESS_ANIMATION_DURATION_MS = 3550;
export const FAILURE_ANIMATION_DURATION_MS = 3550;
export const COUNTDOWN_STEP_MS = 1000;
export const GO_DISPLAY_MS = 600;
